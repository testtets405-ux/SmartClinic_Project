from app import create_app

# ================================================================================
# نقطة الدخول الاحتياطية (Fallback Entry Point) لخوادم مثل Railway
# ================================================================================
# منصات الاستضافة مثل Railway تقوم بشكل افتراضي بالبحث عن ملف باسم main.py
# وتشغيل الأمر: gunicorn main:app
# هذا الملف يضمن عمل التطبيق المحزوم بـ Application Factory بدون أي تعديلات يدوية
# في إعدادات الخادم (Start Command) الخاص بهم.
# ================================================================================

app = create_app()

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=8080)
